{
    // 这是为“Commands”插件添加的按钮配置
    "commands.commands": {
        // 定义一个命令的ID，可以自己随便起名
        "digital-garden.publish": {
            // 指定这个ID要执行的动作：运行一个VS Code任务
            "command": "workbench.action.tasks.runTask",
            // 指定要运行哪个任务，名字必须和 tasks.json 里的 "label" 完全一样
            "args": "Run Publish Script Interactively"
        }
    },
    // 这是在状态栏创建按钮的配置
    "commands.buttons": [
        {
            // 点击按钮时，触发上面定义的那个命令ID
            "command": "digital-garden.publish",
            // 按钮上显示的文字
            "text": "🚀 发布 (交互)",
            // 鼠标悬停在按钮上时显示的提示
            "tooltip": "运行交互式发布脚本",
            // 按钮颜色
            "color": "#42b883",
            // 按钮显示在状态栏左侧
            "alignment": "left",
            // 排序优先级
            "priority": 10
        }
    ]
}
