---
title:
tags:
related:
  - 滑动窗口
data: 2026-02-25T21:33:00
Revision Time: 2026-03-11T13:13:00
---

### 🎯 核心功能 (Purpose)

![[Screenshot_2026-03-11-13-14-26-97_769977972775e0c6b41aa3dfaf766445.jpg]]
对撞指针
![[Screenshot_2026-03-11-13-15-27-63_769977972775e0c6b41aa3dfaf766445.jpg]]
快慢指针
![[Screenshot_2026-03-11-14-12-03-27_769977972775e0c6b41aa3dfaf766445.jpg]]





> 
> ![[Screenshot_2026-02-25-21-32-22-26_149003a2d400f6adb210d7e357a3a646.jpg]]
快慢指针可以使得时间复杂度，从 O (n²)变成 O(n)。

## 状态增量维护

如果你把区间的左边界和右边界想象成两个指针，这种“固定左端点，右端点不断向右扩张”的做法，其实也是大名鼎鼎的**滑动窗口（Sliding Window）和双指针（Two Pointers）**算法的核心前置基础！
它的核心哲学就一句话：“绝不抛弃已经算过的历史数据，把大问题化解为（小问题 + 新增变量）。”

💡 通用核心模板：枚举起点 + 增量扩展
无论题目是求和、计数、还是找特定的字符规律，只要是这种“连续区间”的问题，你都可以套用下面这个模板：
```cpp
int ans = 0; // 最终答案

// 1. 外层循环：枚举所有可能的起点 i
for (int i = 0; i < n; ++i) { 
    
    // 【关键】为当前的起点 i 初始化一个干净的“账本”（状态变量）
    // 注意：这里的账本可以是整型、数组，甚至是一个哈希表（看具体题目需求）
    int count = 0; 
    int sum = 0;   
    
    // 2. 内层循环：终点 j 从起点 i 开始，像贪吃蛇一样一步步向右吃元素
    for (int j = i; j < n && j <= 某个最大边界; ++j) { 
        
        // 【核心操作】增量更新：只把最新吃进来的元素 s[j] 记入账本
        if (满足某种条件(s[j])) {
            count++; 
        }
        sum += s[j]; 
        
        // 3. 结算区：判断当前区间 [i, j] 的状态是否达到了题目的要求
        int current_len = j - i + 1; // 当前区间的长度
        
        // 如果满足条件（比如长度达标、或者和大于某个值）
        if (current_len >= 最小要求 && count >= 标准) {
            ans++; // 记录答案（或者打擂台求 max/min）
        }
        
        // 【注意】如果当前状态已经“彻底无救”了（比如要求全都是数字，但碰到了字母），
        // 可以直接 break 掉内层循环，提前结束这个起点 i 的探索（剪枝优化）。
    }
}
```

🎯 什么时候用？（出现这些“题眼”立刻触发警报）
当你在题目中看到以下几个特征组合出现时，脑子里就应该立刻弹出这个“贪吃蛇增量”模板：
1. 关键词：“子数组”或“子串”
只要题目强调是连续的一段元素，必然涉及到区间的起点和终点，这通常是这套模板的绝对主场。如果题目说是“子序列”（可以不连续），那这套模板就不适用了，通常要考虑动态规划（DP）。
2. 状态具有“可累加性”或“延续性”
你在思考暴力解法时发现：区间 [i, j] 的结果，可以由区间 [i, j-1] 的结果顺手加上第 j 个元素直接得到。比如：
 * 统计区间内某个字母的个数。
 * 求区间元素的和。
 * 记录区间内出现过哪些不同的数字。
3. 数据范围卡在 10^3 到 10^4 之间
如果原数组长度 N 大概在几千左右，标准的 O(N^3) 暴力破解绝对会超时（Time Limit Exceeded）。你需要把时间复杂度降维到 O(N^2)。这套模板正好是 O(N^2) 的。

## 快慢指针模版
​🎯 适用场景：
​题目要求寻找“连续子区间 / 子串”。
​题目让你求“最长 / 最大”、“最短 / 最小” 或者 “满足某种距离/配对的组合数量”。
​暴力双重 for 循环（O(N^2)）会超时，必须降维到一次遍历（O(N)）。
```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// ================= 全局变量区 =================
const int N = 5e5 + 9; // 万物从 1 开始，根据题目要求开满空间
int a[N];              // 原数组（或 string s）
ll ans = 0;            // 最终答案（打擂台或计数，强行养成用 ll 的好习惯）

int main()
{
    // 极致的 I/O 加速
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    
    int n; 
    if(cin >> n) 
    {
        for(int i = 1; i <= n; ++i) cin >> a[i];
        
        int l = 1; // 左指针老老实实从 1 开始，绝不乱动
        
        // 【核心账本】：用于记录窗口 [l, r] 内的特征（比如和、某种字符的数量等）
        // 绝对不要把这个账本定义在 for 循环里面！
        ll window_state = 0; 
        
        // 右指针 r 像贪吃蛇一样，从 1 开始一步步向右吞噬新元素
        for (int r = 1; r <= n; ++r) 
        {
            // ==========================================
            // 步骤 1. 进（充能）：r 踩到了新元素，强行拉进窗口，记入账本！
            // [填写区] 例如：window_state += a[r];
            // ==========================================
            

            // ⚠️⚠️ 【情况 B：求“最短 / 最小”合法区间】专属算分点 ⚠️⚠️
            // 如果题目求最短，走到这里说明刚刚吃进来的 r 可能让窗口达标了。
            // 趁着还没进 while 吐出元素，先用 ans = min(ans, r - l + 1); 记录一下！
            

            // ==========================================
            // 步骤 2. 判：触发拉响警报的临界条件（安检门）
            // 什么时候必须收缩窗口？比如和超标了、遇到重复字符了、距离够长了
            // ==========================================
            while( /* [填写区] 触发收缩的条件，例如 window_state > target 或 r - l + 1 >= k */ ) 
            {
                // 步骤 3. 算（内部状态更新 / 计数类特供）：
                // ⚠️⚠️ 【情况 C：配对 / 计数类】专属查库存点 ⚠️⚠️
                // 这是 l 滚出窗口前的“毕业安检”：看看 l 踩到的是不是我们要的特征？
                // [填写区] 例如：if(a[l] == 目标) valid_cnt++;
                

                // 步骤 4. 出：毁尸灭迹！将 a[l] 的影响从账本中残忍抹去！
                // [填写区] 例如：window_state -= a[l];
                
                
                // 左指针 l 往前挪一步，完成一次收缩（这一步死都不能删）
                l++;
            }
            
            // ==========================================
            // 步骤 5. 算（外部统计答案）：
            // ==========================================
            
            // ⚠️⚠️ 【情况 A：求“最长 / 最大”合法区间】专属算分点 ⚠️⚠️
            // 因为 while 循环就像个清洁工，把所有“不合法”的都吐掉了。
            // 能活着走到这一行的窗口 [l, r] 绝对是合法的！
            // [填写区] 例如：ans = max(ans, (ll)(r - l + 1));
            
            
            // ⚠️⚠️ 【情况 C：配对 / 计数类】专属算分点 ⚠️⚠️
            // 此时 r 踩到了目标元素，直接和内部 while 收集到的合法库存进行组合结算！
            // [填写区] 例如：if(a[r] == 目标2) ans += valid_cnt;

        }
        
        cout << ans << '\n';
    }
    return 0;
}

```

发现是滑动窗口后，只需要灵魂三问：

1. ​**账本记什么？** （和？个数？哈希表查重？）
2. ​**while** **的警报条件是什么？** （什么时候必须让 l 往右跑？）
3. ​**我是哪种情况（A/B/C）？**
    - ​求最长 👉 **算在** **while** **外面下面**（情况 A）。
    - ​求最短 👉 **算在** **while** **外面上面**（情况 B）。
    - ​数组合/配对 👉 **while** **里面收库存，****while** **外面算总账**（情况 C，即《子串简写》）。

### ✍️ 代码
对撞指针例题：

![[Pasted image 20260311164227.png]]

```cpp
#include <bits/stdc++.h>

using namespace std;

const int N = 1e6+9;

char s[N];

int main()

{

  ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);

  cin >> s+1;

  

  int n = strlen(s+1);

  int l = 1,r = n;

  bool ans = true;

  while(l<r && ans)

  {

    if(s[l] != s[r]) ans = false;

    l++,r--;

  }

  cout << (ans ? 'Y' : 'N') <<'\n';

  return 0;

}
```



快慢指针（滑动窗口）模板：
```cpp
const int N =  ;
int a[N];
int main()
{
 int n;cin >> n;
    int l = 1;
    long long ans = 0;   
    // [定义状态变量]：比如 sum, count, 用于记录窗口内的特征
    State window_state = 0; 

    for (int r = 1; r <= n; ++r) {
        // 1. 进（充能）：将 a[r] 加入 后对window_state的影响
         

        // 2. 判：当窗口状态满足临界条件
        while(ccondition(window_state)) {
            
            // 3. 算（统计答案）：
            // 注意！根据题目求的是“最大/最短区间”还是“子串数量”，
            // 这行代码可能在 while 内部，也可能在 while 外部。
            ans = update_ans(ans, l, r); 

            // 4. 出：将 a[l] 移出对window_state的影响，准备向右滑动
            l++;
        }
        // (如果是求最长合法区间等问题，"算"的步骤通常放在这里)
    }
    return ans;
}

```

快慢指针（滑动窗口）例题：

![[Pasted image 20260311172137.png]]


```cpp
#include <bits/stdc++.h>

using namespace std;

const int N = 1e5+9;

int a[N]; 

int main()

{
  ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
  int n,s;cin >> n >> s;

  int l =1;

  int ans = n+1;

  for(int i = 1;i <= n;++i) cin >> a[i];

  int count = 0;

  for(int r = 1;r<=n;++r)

  {

    count += a[r];

    while(count >= s)

    {

      ans = min(ans,r-l+1);

      count -= a[l];

      l++;

    }

  }

  cout << (ans <= n ? ans : 0) <<'\n'; 

  return 0;

}
```

例题2：

![[Pasted image 20260311175237.png]]

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1e4+9;
int a[N];

int main()

{

  ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);

  int n,m,k;cin >> n >> m >> k;

  for(int i = 1;i <= n;++i) cin >> a[i]; 

  int l =1;

  int cnt = 0;

  int ans = 0;

  for(int r = 1;r<=n;++r)

  {

    if(a[r] >= m)

    {

      cnt++;

    }

    while(cnt >= k) 

    {

      ans += n - r + 1;

      if(a[l] >= m) 

      {

        cnt--;

      }

      l++;

    }

  }

  cout << ans <<'\n';

  return 0;

}
```